package practica2.ejercicio3;

import practica2.BinaryTree;

public class ContadorArbol {
	public BinaryTree<Integer> tree;
	
	public Integer[] numerosPares(BinaryTree<Integer> tree) {					
		if (this.tree != null) {
			numerosPares(tree.getLeftChild());
			if (tree.getData() %2 == 0)
				
		}
	}
	
}
